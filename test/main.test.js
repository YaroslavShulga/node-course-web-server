var assert = require("assert");
var expect = require("chai").expect;

describe("Main file test", () => {
	it("equal", () => {
		expect(5, "mother fuck").to.be.equal(5);
		expect("hey").to.be.a("string");
		expect(true).is.true;
	});

	it("write this case");
});
